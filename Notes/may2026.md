19 May 2026

* Started gradual migration from JavaScript to TypeScript while still supporting `.js` and `.jsx` files.

Most important `tsconfig` additions for smooth migration:

```json
"allowJs": true,
"checkJs": false
```

Why these are important:

* Allows existing JS files to continue working

* Prevents TypeScript from validating all legacy JS files

* Makes gradual migration easier and less painful

* Added `TestTsx.tsx` component successfully and verified TSX setup is working.

* Vibecoded a production-grade architecture reference project using [TanStack Query](https://tanstack.com/query/latest?utm_source=chatgpt.com) for future learning/reference.

* Added [TanStack Query](https://tanstack.com/query/latest?utm_source=chatgpt.com) dependency.

* Fixed routing issue in Vite config by commenting out:

```ts
// base: '/vite-react-frontend-app/',
```

After removing/commenting this line, routing started working correctly again.

Current config:

```ts
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': '/src' },
  },
})
```

* Learned:

  * DTOs (Data Transfer Objects) are used for transferring data between application layers.
  * Models represent application business logic and internal data structure.

Next Tasks:

* Understand multiple providers setup inside `AppProviders` and their hierarchy
* Combine Secure Notes routes/pages
* Reuse existing Secure Notes authentication logic
* Merge both functional flows properly

Future Scope:

* Add authentication module
* Add protected routes
* Add TanStack Query mutations
* Add global toast system
* Add testing setup using Vitest + RTL
* Add form handling and validation


21st MAy 
adding auth modules and protected routes.
today learned about centralized auth proess, data management. and also about proetced routes an placement in app. 
added some comment on data flow and placement of custom hooks which are critical on app loading.

Next Tasks:

* Combine Secure Notes routes/pages
* Reuse existing Secure Notes authentication logic
* shwo ounts in heaer so that you can learn data fethching placement. 
* Merge both functional flows properly
* Fix Dark Mode in plp, pdp pages (specially in inputs)

Future Scope:

* Add TanStack Query mutations
* Add global toast system
* Add testing setup using Vitest + RTL
* Add form handling and validation


